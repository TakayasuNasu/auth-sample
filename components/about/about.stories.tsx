import React from 'react'
import { Story, Meta } from '@storybook/react'
import About from './index'

export default {
  title: 'Organisms/About',
  component: About,
} as Meta

export const AboutComponent = (args) => <About />
